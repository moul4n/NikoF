# Project Context

- **Owner:** Jason Fletcher
- **Project:** Local-only anime companion with interchangeable VRM characters, local voice pipeline, persistent memory, and AI-authored animation scripts
- **Stack:** Python backend, FastAPI/Starlette, React + TypeScript + Vite, three.js + three-vrm, Faster-Whisper, Ollama/llama.cpp, GPT-SoVITS, SQLite, Chroma/FAISS
- **Created:** 2026-05-14T08:57:41.6820932+01:00

## Learnings

- The backend is the central orchestrator for LLM, STT, TTS, memory, character config, and animation messaging.
- Character and animation behavior should be loaded from config files, not baked into service logic.
- Phase 0 backend work should stay framework-light: keep route shape coherent for FastAPI, but let service contracts and filesystem-backed character loading land before provider integrations.
- Character package access belongs behind a manifest source boundary so route handlers never need to know about `assets/characters/{character_id}/manifest.json` layout details.
- Bootstrap and backend settings should share one local-path contract: `NIKOF_LOCAL_ROOT`, `NIKOF_MODELS_ROOT`, `NIKOF_LLM_MODELS_ROOT`, `NIKOF_STT_MODELS_ROOT`, `NIKOF_TTS_MODELS_ROOT`, `NIKOF_EMBEDDINGS_ROOT`, `NIKOF_PROVIDERS_ROOT`, and `NIKOF_CACHE_ROOT`.
- Machine-local bootstrap reports and session env helpers belong under `.local/bootstrap/` so they stay disposable and Git-ignored, while heavyweight provider payloads still default to `%LOCALAPPDATA%\NikoF`.
- Stage 1 route payloads should normalize manifest data down to stable summaries only: include `schema_version`, `character_id`, `display_name`, `identity_source`, `vrm_spec_version`, `supported_states`, and `shared_animation_set`, but do not surface asset-version bookkeeping or raw manifest paths.
- Active-character control is the Stage 1 session boundary: `GET` and `PUT /session/active-character` should return a dedicated response envelope with the selected character summary plus a normalized `session_event`, so later SSE or WebSocket transport can reuse the same payload shape.
- For frontend-facing Stage 1 reads, `GET /characters` should use a catalog envelope with `schema_version`, `active_character_id`, and normalized character summaries instead of a bare array.
- Invalid active-character writes should keep the existing active summary, return the same normalized response envelope as successful reads and writes, and fail through a stable `selection` object plus HTTP 400 rather than a leaked manifest lookup error.
- Scaffold `/health` should stay provider-agnostic and report diagnostics-lite through named storage probes and remediation notes, not absolute filesystem paths or provider-specific payloads.
- The first backend-owned event store should stay provider-agnostic and deterministic: persist canonical `session` and `speech.lifecycle` events in per-session, per-stream order and reuse the existing envelope fields (`event_id`, `sequence`, `cursor`, `event`) rather than inventing a second transport shape.
- The `GET /session/speech-lifecycle` read surface can add cursor-based reads without changing its snapshot envelope by reading ordered events from the backend-owned store and keeping `next_cursor` as the stream-local append position.
- The safest first cut against the backend router monolith is the session transport seam: move `/session/animation`, `/session/lifecycle-state`, and `/session/speech-lifecycle` registration plus SSE framing into a dedicated API helper module while keeping `build_api_router()` as the composition point.
- Router-local helper names such as `_build_session_animation_response` and `_build_speech_lifecycle_sse_frame` already leak into backend tests and contract tooling, so internal extractions should preserve those names as thin compatibility wrappers until the callers are migrated.
- The next safe router cleanup slice after session transport is response shaping: move catalog, active-character, health, speech-contract example, and dataclass-serialization builders into a focused API helper module while keeping leaked router helper names as compatibility wrappers.
- Stage 1 backend stability guards must project only the Stage 1 seam from `build_api_contract_snapshot()`: keep `GET /health`, `GET /characters`, and `GET` plus `PUT /session/active-character`, and scope catalog examples to the tracked scaffold manifests so extracted session routes and local imported characters do not silently widen the Stage 1 baselines.
- The next safe router-owned seam after response shaping is operator-command publication: move `/session/operator-command` registration and turn-publication branching into a dedicated API module while keeping `build_api_router()` as the composition point and leaving the route signature and response envelope unchanged.
- 2026-05-17: The next safe router-owned seam after operator-command publication is active-character session selection: keep `GET` and `PUT /session/active-character` registration in `backend/app/api/active_character_routes.py`, leave `build_api_router()` as the composition entrypoint, preserve the existing selection and `session_event` envelope shape, and add the new module to the `backend-stage1-contracts` plus `backend-stage1-payload-surface` tracked inputs so Stage 1 modularization does not look like a false regression.
- 2026-05-17: The next safe remaining Stage 1 router seam after active-character selection is the simple read-route pair. Keep `GET /health` and `GET /characters` registration in `backend/app/api/read_routes.py`, leave `build_api_router()` as the composition entrypoint, preserve the current health and catalog envelopes, and update only `backend-stage1-contracts` plus `backend-stage1-payload-surface` tracked inputs so the modularization changes seam ownership without widening Stage 1 behavior.
- 2026-05-17: After the route clusters themselves moved out, the next low-risk router seam is registration composition. Keep `build_api_router()` as the public seam, move the repeated route-registration wiring into `backend/app/api/router_composition.py`, preserve the leaked compatibility helpers in `router.py`, and add the helper to the route-focused backend stability tracked inputs so modularization does not look like a false regression.
- When a backend stability harness source-scans router decorators, extracted route modules must be added to that scenario's tracked inputs and marker scan or the modularization will look like a false contract regression.
- Router modularization slices stay scoped to backend API composition and must not touch the temporary external three-vrm git download.
- 2026-05-17: `backend-session-animation-live-delivery` must track `backend/app/api/session_routes.py` alongside `backend/app/api/router_composition.py` so the stability seam follows the real extracted session-transport owner instead of only the composition helper.
- 2026-05-17: The next low-risk backend router reduction after route-registration composition is default service construction. Move the stub service assembly into `backend/app/api/router_composition.py`, but keep `build_api_router()` as the public seam and preserve the router-local `_build_services()` tuple wrapper because focused backend tests patch that symbol directly.
- 2026-05-17: After default service construction moved out, the next low-risk router reduction is shared contract-snapshot glue. Keep `build_api_contract_snapshot()` public in `backend/app/api/router.py`, move the snapshot assembly into `backend/app/api/router_composition.py`, and leave the existing backend stability tracked inputs unchanged because the composition helper is already the seam owner.
- 2026-05-17: After contract-snapshot assembly moved out, the next low-risk router trim is the static route-definition shell. Keep `build_api_router()` and `build_api_contract_snapshot()` public in `backend/app/api/router.py`, move `RouteDefinition`, `RouterShell`, and the canonical route-definition list into `backend/app/api/router_composition.py`, preserve router-level compatibility exports plus `_route_definitions()`, and add `backend/app/api/router_composition.py` to `backend-speech-contracts` so the stability seam keeps following the real owner.
- 2026-05-17T23:26:53.4019424+01:00: The next low-risk backend router trim after the route-definition shell is the public entrypoint composition glue itself. Keep `build_api_router()` and `build_api_contract_snapshot()` public in `backend/app/api/router.py`, preserve patched wrappers like `_build_services()` and `_build_session_animation_live_delivery_service()`, and delegate the remaining runtime assembly plus FastAPI fallback wiring into `compose_api_router()` and `compose_api_contract_snapshot()` in `backend/app/api/router_composition.py`. Focused validation stayed on the backend wrapper seam with `python -m unittest tests.test_animation_service.SessionAnimationContractSnapshotTests tests.test_event_store` passing.
- 2026-05-17T23:46:00+01:00: The last safe constructor-only glue in `backend/app/api/router.py` was the direct `DefaultAnimationService()` and `InMemorySessionAnimationLiveDeliveryService()` instantiation. Move those constructors into `backend/app/api/router_composition.py` behind `build_default_animation_service()` and `build_default_session_animation_live_delivery_service()`, but keep `_build_animation_service()` and `_build_session_animation_live_delivery_service()` as router-local wrappers because focused backend tests still patch the live-delivery wrapper directly. After that extraction, `router.py` is effectively at the minimum safe wrapper surface: the remaining code is the public `build_api_router()` and `build_api_contract_snapshot()` seams plus compatibility wrappers that current tests and contract tooling still rely on.
- 2026-05-18T00:26:00+01:00: Bootstrap prerequisite checks now need to track the backend's real runtime bindings rather than coarse model folders. For local LLM and TTS setup, keep one shared contract across `scripts/bootstrap/` and `backend/app/core/settings.py`: Ollama model guidance is exposed as a safe `ollama pull llama3.1:8b` hook plus a repo-facing readiness marker under `NIKOF_LLM_MODELS_ROOT\ollama-llama3.1-8b\.installed.json`, while GPT-SoVITS stays manual-only with explicit payload and provider-entrypoint paths under `NIKOF_TTS_MODELS_ROOT\gpt-sovits` and `NIKOF_PROVIDERS_ROOT\tts\gpt-sovits`. `app.dev_server` should warn from that same contract and continue in degraded mode instead of hiding the missing runtime steps.
- 2026-05-18T07:22:00Z: The durable fresh-machine pattern for the current local LLM and TTS lane is hook-backed local manifests, not copied vendor trees. Keep bootstrap manual hooks responsible for scaffolding `runtime.json` and `install-plan.json` under `NIKOF_LLM_MODELS_ROOT`, `NIKOF_TTS_MODELS_ROOT`, and `NIKOF_PROVIDERS_ROOT`, let `backend/app/core/settings.py` and `app.dev_server` surface those exact paths, and let GPT-SoVITS read only machine-local runtime overrides such as `entrypoint`, `python_executable`, and `timeout_seconds` from `NIKOF_PROVIDERS_ROOT\tts\gpt-sovits\runtime.json`.
- 2026-05-18T06:26:06Z: Public speech payloads should keep raw synthesized audio paths inside canonical `speech.lifecycle` storage only, project frontend-facing `audio_reference` values to `/api/session/speech-artifacts/{event_id}/audio`, and resolve that route only through the current session's canonical speech event plus allowed local artifact roots such as `NIKOF_TTS_MODELS_ROOT` and `NIKOF_CACHE_ROOT`.
- 2026-05-18T10:14:35Z: `/health` prerequisite truth should stay a thin public projection of `get_startup_runtime_prerequisites()`: expose only lane `state` plus blocker `id`, `status`, and `summary`, and do not leak startup-only paths, evidence, remediation prose, or provider internals into the frontend-facing diagnostics surface.
- 2026-05-18: Once `/health` carries `diagnostics.prerequisite_lanes`, protect it through the existing Stage 1 backend and frontend guards instead of inventing another scenario. Preserve the field inside `ConvertTo-BackendStage1ScopedSnapshot`, track `backend/app/api/response_builders.py` in the backend Stage 1 seams, and keep frontend health-lane ownership on `surfaceShellPresentation.tsx` alongside the extracted bridge owners.
- 2026-05-19: Phase 3 animation command protocol landed. The SSE endpoint at `/session/animation` now emits dual events: the existing `session.animation` snapshot plus a new `animation.command` thin envelope containing actionable VRMA playback commands (`play_animation`, `stop_animation`, `crossfade`, `set_expression`, `set_lookat`). The `AnimationCommandTranslator` in `backend/app/services/animation_commands.py` converts resolved `AnimationCommand` objects into frontend-facing thin commands, maintaining per-session base-clip state to produce crossfade commands on layer transitions. All 77 backend tests pass with backward compatibility preserved.
