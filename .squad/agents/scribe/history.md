# Project Context

- **Project:** NikoF
- **Created:** 2026-05-14

## Core Context

Agent Scribe initialized and ready for work.

## Recent Updates

📌 2026-05-18T12:12:53Z: Pre-checked `.squad/decisions.md` and `.squad/decisions/inbox/`, merged the pending Trinity idle-finger hardening approval into `.squad/decisions.md`, cleared the processed inbox file, wrote the hardening session and orchestration records, updated Mouse and Trinity continuity, and left `.squad/identity/now.md` unchanged because active coordination still sits on the prerequisite summary proof lane.

📌 2026-05-18T11:51:02Z: Merged the approved idle-finger fallback and approval inbox decisions into `.squad/decisions.md`, cleared the processed inbox files after confirming the remaining GPT-SoVITS note was a stale duplicate already canonicalized, wrote the approved official-idle finger session and orchestration records, appended the missing official-idle finger-fallback continuity to Switch history, and left `.squad/identity/now.md` unchanged because the active coordination lane still sits on the prerequisite summary proof seam.

📌 2026-05-18T11:14:18Z: Merged the approved idle-wrist inbox decisions into `.squad/decisions.md`, cleared the processed wrist inbox files after confirming the remaining prerequisite-card inbox files were stale duplicates already canonicalized, wrote the approved official-idle wrist session and orchestration records, verified existing Mouse and Switch wrist learnings already covered the active seam, and left `.squad/identity/now.md` unchanged because the active coordination lane still sits on the prerequisite summary proof seam.

📌 2026-05-18T10:58:02Z: Merged the pending idle-elbow and prerequisite-card inbox decisions into `.squad/decisions.md`, cleared the processed inbox files, wrote the approved official-idle elbow session and orchestration records, appended the rendered prerequisite-card harness guidance to Switch history, and left `.squad/identity/now.md` unchanged because the active coordination lane still sits on the prerequisite summary proof seam.

📌 2026-05-18T10:39:11Z: Pre-checked `.squad/decisions.md` and `.squad/decisions/inbox/`, confirmed the idle-grounding and health-surface inbox items were already canonicalized with the inbox empty, then wrote the approved idle-foot-grounding session and orchestration records and appended Tank's missing `/health` prerequisite continuity while leaving `.squad/identity/now.md` unchanged.

📌 2026-05-18T07:31:16.8111521Z: Checked the empty decisions inbox and logged the local-only GPT-SoVITS scaffold follow-up: Tank's non-destructive hooks created machine-local runtime and install-plan manifests under the managed TTS roots, Link confirmed the approved payload, speaker and reference assets, and provider entrypoint are still missing, `tts_preview` remains degraded rather than live, and `now.md` stayed unchanged because active focus did not materially move.

📌 2026-05-18: Logged the idle proof follow-up after crash recovery resumed the shoulder slice from in-worktree changes, closed that proof boundary at shoulders only, then widened official `idle.default` playback to hips and chest pitch-roll with idle-only axis scaling; later reviewer passes forced the shared-official-path punch proof and raw-versus-normalized snapshot guard, leaving no high or medium implementation findings and only low proof-quality risk.

📌 2026-05-18T07:25:39Z: Merged the pending TTS resume inbox into `.squad/decisions.md`, collapsed the duplicate user directive into one recorded decision, cleared the processed inbox files, shifted `.squad/identity/now.md` to the local-only LLM and TTS resume lane, wrote the batch session and orchestration records, and appended the missing bootstrap-contract continuity to Mouse history while leaving existing 2026-05-18 Link, Tank, Switch, and Trinity learnings in place.

📌 2026-05-17: Merged Tank's pending backend router minimum-safe-wrapper inbox decision into `.squad/decisions.md`, updated `now.md` to record that the backend router cleanup pass is green and that `backend/app/api/router.py` is now near the minimum safe wrapper surface, and logged Mouse's no-blocker review with the focused unittest proof for `_build_services()` and `_build_session_animation_live_delivery_service()` while keeping the remaining wrappers explicitly unproven as external compatibility points.

📌 2026-05-17: Merged the pending cleanup inbox decisions for `frontend/src/app/useRuntimePlaybackSelection.ts` and the remaining `backend/app/api/router.py` entrypoint glue moved behind `backend/app/api/router_composition.py`; updated `now.md` to mark the pair green after Mouse's no-regression review; logged the focused rerun with `frontend-shell-split-surface`, `frontend-avatar-idle-default-runtime`, `backend-stage1-contracts`, `backend-speech-contracts`, and the focused backend contract/event test slice all passing.

📌 2026-05-17: Logged the idle-route review closeout after two proof revisions were rejected before Tank hardened the `idle.default` gate with route extraction, real-route proof, a non-aliased fake rig, and an explicit fallback-negative check; the landed motion slice changed only official idle hips and chest retain weights, follow-up review narrowed official idle stability ownership to `idle.default`, the focused `npm exec -- tsc --noEmit`, `frontend-avatar-idle-default-runtime`, and `frontend-punch-debug-runtime` checks passed, and the remaining risk stayed live real-VRM fidelity rather than seam coverage.

📌 2026-05-17: Merged the pending App runtime-shell and backend route-definition-shell inbox decisions into `.squad/decisions.md`; updated `now.md` to record the baseline-hygiene corrections that restored `backend-speech-contracts` and narrowed `frontend-avatar-idle-default-runtime` back to the App plus `useAvatarRuntimeShell.ts` seam; logged the green follow-up with `backend-speech-contracts`, `frontend-avatar-idle-default-runtime`, `frontend-shell-split-surface`, and the focused backend animation-service unit slice all passing.

📌 2026-05-17: Merged the pending App shell-orchestration and backend contract-snapshot composition inbox decisions into `.squad/decisions.md`; updated `now.md` to mark both ordered cleanup slices green after Mouse's no-regression review; logged the follow-up with `frontend-shell-split-surface`, `backend-stage1-contracts`, `backend-stage1-payload-surface`, and `tests.test_animation_service.SessionAnimationContractSnapshotTests` all passing.

📌 2026-05-17: Logged the narrow torso review closeout after the harness-fix pass closed the remaining idle torso seam findings, Trinity and Mouse's final re-review found no torso-slice defects left, and the only residual risk stayed the missing live VRM rendered-pose assertion on the official `idle.default` path.

📌 2026-05-17: Merged the pending App surface-shell branch and backend router service-construction inbox decisions into `.squad/decisions.md`; updated `now.md` to mark the ordered frontend App reduction and backend router service-glue reduction green; logged the focused rerun with `frontend-shell-split-surface`, `backend-operator-command-surface`, and the targeted backend unittest slice all passing.

📌 2026-05-17: Logged the torso-only `idle.default` implementation follow-up after Switch kept the change inside the allowed frontend runtime and stability seam, landing torso retain coverage in `officialPunchClipPlayback.ts` plus idle-specific spine, chest, and upper-chest fallback parity overrides in `humanoidChannelPlayback.ts`; Coordinator verified the focused validations stayed green with `npm exec -- tsc --noEmit`, `frontend-avatar-idle-default-runtime`, and `frontend-punch-debug-runtime` all passing.

📌 2026-05-17: Merged the pending cleanup inbox decisions for `frontend/src/app/surfaceShellPresentation.tsx`, `backend/app/api/router_composition.py`, and the official idle torso retain seam into `.squad/decisions.md`; updated `now.md` to mark the surface presentation and router composition slices green after correcting `backend-session-animation-live-delivery` to keep tracking `backend/app/api/session_routes.py`; logged the focused five-scenario rerun and kept the official-route live verification limit separate from the durable decision.

📌 2026-05-17: Merged the pending cleanup inbox decisions for `frontend/src/app/useSessionAnimationState.ts`, `frontend/src/app/useSpeechPlaybackBridge.ts`, `backend/app/api/active_character_routes.py`, and `backend/app/api/read_routes.py`; updated `now.md` to mark the latest App reductions and Stage 1 router route extractions green after narrow frontend and backend seam reruns; logged the follow-up with the next cleanup seams kept local to App orchestration and remaining router composition work.

📌 2026-05-17: Logged the lead-planning follow-up for the next animation step: Jason selected the planning-led path, Trinity isolated a narrow official `idle.default` torso slice away from monolith cleanup, the allowed implementation surface now centers on `officialPunchClipPlayback.ts` with `humanoidChannelPlayback.ts` only as a fallback parity seam if hips or another torso requirement escape the official route, torso tuning order stays spine then hips then chest then upper chest, punch remains a discriminator rather than the fix target, and the runtime guard stays `frontend-avatar-idle-default-runtime`.

📌 2026-05-17: Logged the animation research reconstruction after Switch, Trinity, Mouse, and Coordinator reassembled the shoulder and head progress path from official `idle.default` stability, bone-local comparison playback, upper-arm sign and offset fixes, and the current spine-signal evidence. Continuity now frames the next body validation order as spine first, then hips, chest and upper chest, shoulders and upper arms re-check, elbows and lower arms, wrists and hands, upper and lower legs, feet and toes, and finally fingers.

📌 2026-05-17: Merged the remaining cleanup inbox decisions for `frontend/src/app/useSpeechLifecycleState.ts` and `backend/app/api/operator_routes.py`, updated `now.md` to mark both cleanup slices green with the refreshed frontend and backend seam checks, logged the stale `$routes` analyzer warning as already-resolved harness cleanup, and captured App session-animation consumption or another router cluster as the next likely cleanup seam.

📌 2026-05-17: Merged the pending cleanup inbox decisions for `frontend/src/app/useCharacterShellState.ts`, `backend/app/api/response_builders.py`, and the scoped Stage 1 backend stability projection into `.squad/decisions.md`; updated `now.md` to mark the cleanup pass green after the narrow frontend and backend harness corrections; and logged Mouse's final four-scenario rerun with all cleanup-relevant guards passing.

📌 2026-05-17: Merged the pending decision inbox into `.squad/decisions.md`, shifted `now.md` from neutral-stance stabilization to external-review-guided cleanup and modularization, logged the first safe `App.tsx` and `router.py` extraction slices, and recorded the follow-up alias cleanup plus Mouse's no-regression review.

📌 Team initialized on 2026-05-14

📌 2026-05-14T08:57:41.6820932+01:00: Planning artifacts landed in README.md, docs/ARCHITECTURE.md, and docs/IMPLEMENTATION_PLAN.md

📌 2026-05-14: Repaired squad continuity state by restoring missing `.squad/log/`, `.squad/orchestration-log/`, and `.squad/decisions/inbox/`, and removed an accidental pasted patch block from Mouse history.

📌 2026-05-14: Aligned Scribe and Ralph support-role charters with the current squad roster so Scribe is documented as the session logger and continuity maintainer, and Ralph as the work monitor.

📌 2026-05-14T12:15:50.9004620Z: Merged the pending Scribe inbox decisions into `.squad/decisions.md` and logged the continuity pass, including the Stage 1 bridge audit finding of a frontend/backend contract mismatch.

📌 2026-05-14T12:30:00.8688200Z: Logged the Stage 1 bridge repair continuity pass after the frontend/backend bridge envelope mismatch and rejection rollback path were fixed, and after the `frontend-stage1-bridge-surface` stability scenario was extended to cover the rejection path.

📌 2026-05-14: Merged the remaining frontend-stage1-bridge rollback assertion inbox decision into `.squad/decisions.md` and cleared the decision inbox.

📌 2026-05-14T13:46:07.7392187+01:00: Updated squad state after confirming the repaired frontend build passes, the Stage 1 bridge stability scenario is green, and the provider-agnostic backend speech-contract slice is now baseline-covered.

📌 2026-05-14: Logged the next-step batch after provider-agnostic speech service interfaces, the `speech.lifecycle` transport snapshot contract, and the runtime-executed frontend/backend character-flow check landed, without adding any new decision record.

📌 2026-05-14: Logged the continuity pass after the Faster-Whisper and GPT-SoVITS adapter shells, the backend `GET /session/speech-lifecycle` read surface, and the frontend runtime speech snapshot proof landed, without adding a new decision record.

📌 2026-05-15: Logged the animation continuity pass after the backend animation contract became a real resolution seam, the promoted semantic DSL target schema was documented, and staged-versus-promoted animation boundaries were locked by the stability baseline.

📌 2026-05-15: Logged the idle.default continuity pass after the Unity batch export produced the generated shared runtime payload and semantic candidate, the avatar viewer seeded idle.default as its default base layer, and `frontend` returned to a green build.

📌 2026-05-15: Logged the backend-owned animation control continuity pass after `/session/animation` became the frontend shell's normal default-idle source, with the local `idle.default` clone retained only as a fetch-failure fallback.

📌 2026-05-15: Logged the lifecycle-driven animation control batch after backend session animation began resolving from canonical lifecycle state and the frontend started updating backend lifecycle state instead of owning active base animation mode locally.

📌 2026-05-15: Checked the empty decision inbox and logged the current web-first live animation seam only: the control shell now derives conversation lifecycle from canonical speech state, pushes it through `PUT /session/lifecycle-state`, and plays the backend-selected `idle.default`, `listen.loop`, or `speak.loop` command from the backend animation snapshot, with the local idle clone left as failure fallback only.

📌 2026-05-15: Merged the pending Switch humanoid-playback decisions into `.squad/decisions.md`, updated current continuity to mark arm calibration no longer blocked after live `/display/` verification, and logged the remaining issue as web-playback polish and exporter fidelity rather than the visible but unrelated backend speech lifecycle 422 responses.

📌 2026-05-15: Logged the refresh-regression continuity pass after live browser review showed the arms rising again on refresh, the loaded debug snapshot proved the earlier sign-flip edit was still present, and the current blocker narrowed from lost refresh state to remaining elbow/arm shaping and exporter fidelity after the upper-arm input offset was removed.

📌 2026-05-15: Logged the exporter-side elbow continuity pass after the user chose the exporter path, Switch proved the source clip still carried usable lower-arm data, the Unity batch exporter derived explicit elbow-flex channels and refreshed `idle.default`, `listen.loop`, and `speak.loop`, and live `/display/` verification showed elbow-flex plus forearm-twist lower-arm bindings in the runtime debug snapshot while backend speech lifecycle 422 responses stayed out of scope.

📌 2026-05-15: Logged the full-bone audit continuity pass after the user chose option 1, Switch verified the generated runtime sidecars already carry broader humanoid data than the browser was consuming, the backend remained semantic-only, the frontend payload path was confirmed to preserve exported channels, conservative finger stretch bindings landed and were live-verified, and the remaining unbound humanoid regions were explicitly left out of scope.

📌 2026-05-15: Merged the pending Switch finger-spread inbox decision and the paired user directive into `.squad/decisions.md`, updated current continuity to reflect that both finger stretch and spread are now bound in the web viewer, appended concise hand-slice learnings, and logged the implementation plus live `/display/` verification with 10 active spread bindings while backend speech lifecycle 422 responses remained out of scope.

📌 2026-05-15: Merged Jason's direct browser A/B approval and the pending Switch dev-switcher inbox decision into `.squad/decisions.md`, updated current continuity to reflect the landed dev-only display animation override plus punch validation on the 4174 surface, appended concise frontend-validation learnings, and logged that forced `gesture.punch.once` confirmed wrist and lower-arm quaternion delivery without isolating one new arm-chain defect beyond known weighting concerns while backend speech lifecycle 422 responses remained out of scope.

📌 2026-05-15T20:57:25.2199054+01:00: Merged the durable Switch root-orientation inbox decision into `.squad/decisions.md`, updated current continuity to record that the punch turn-away investigation falsified a general local-bone Z-axis reversal and instead landed at authored root transform playback in the frontend root seam, appended concise cross-agent learnings, and logged the remaining risk as the still-offline backend-driven `127.0.0.1:8000` verification path.

📌 2026-05-15T21:02:44.9247142+01:00: Logged the focused local validation follow-up after the frontend root-transform playback fix: `npm exec -- tsc --noEmit` stayed green, the `frontend-punch-debug-runtime` stability scenario isolated a single `browser_rotation_space` baseline drift from `vrm_normalized_pose_delta` to `vrm_rendered_raw_bone_local_rotation`, the focused punch baseline was refreshed and rerun cleanly, and the next explicit step remains backend-driven verification once the backend path is online.

📌 2026-05-15T23:14:51.4365825+01:00: Logged the lifecycle write-path diagnosis follow-up after Tank isolated the intermittent `PUT /session/lifecycle-state` hang to stale uvicorn listener ownership on `127.0.0.1:8000` rather than the backend route handler, confirmed that replacing the bad listener restored direct `:8000` read and write probes with `debug.punch` and `gesture.punch.once`, and updated continuity so the remaining live issue is now the frontend `127.0.0.1:5173` proxied `PUT` path returning `404`.

📌 2026-05-16: Logged the idle-follow-up continuity pass after Switch added a dev-only front/side profile toggle for the display surface and revalidated `frontend` with `npm exec -- tsc --noEmit`, while Tank confirmed that the current generation/export repair path is still specific to `gesture.punch.once`; continuity now frames the next step as a product choice between widening that repair path to `idle.default` or swapping in a better idle source clip.

📌 2026-05-17: Logged the neutral stance stability follow-up after Switch gated display overrides and the neutral override effect on avatar runtime readiness, replaced the old one-shot camera framing with current-bounds-based refit on load, resize, and profile changes, and revalidated `frontend` with `npm exec -- tsc --noEmit`; Mouse confirmed that holding neutral during loading no longer blanks the page and that framing improved in both front and side profile, so continuity now treats the remaining issue as the neutral clip still being a T-pose rather than a relaxed standing baseline.

## Learnings

Initial setup complete.

- 2026-05-18: When a browser-adjacent hardening pass lands after the runtime approval seam is already green, continuity should record the display-side proof as an adjunct that answers the remaining real-VRM question while keeping the original executable seam authoritative. If shell-split snapshots drift only because tracked inputs widened, log that as collateral churn rather than a new ownership regression.

- 2026-05-18: When `.squad/decisions/inbox/` still contains files that were already canonically merged into `.squad/decisions.md`, confirm the duplicate against the active ledger first, clear the stale inbox files, and log only the new durable decision so the ledger stays append-only without duplicate active entries.

- 2026-05-18: When a local-only TTS batch lands across backend provider discovery, bootstrap prerequisites, and frontend playback cleanup, continuity should keep the durable remaining seam explicit: a backend-safe or session-scoped audio artifact path is still required once canonical lifecycle status, startup hints, and reply-gated synthesis are green. Do not let raw local filesystem `audio_reference` values get rewritten as if the lane is already end-to-end complete.

- 2026-05-17: When a cleanup follow-up exposes baseline-hygiene drift, continuity should record which guard absorbed real contract drift versus unrelated implementation churn, restore each guard to the intended ownership seam, and note the focused unit guard that locks the corrected projection before marking the slice green.

- 2026-05-17: When a backend cleanup slice moves shared contract-snapshot glue behind an already-tracked composition seam, continuity should keep the tracked inputs unchanged and log the preserved public snapshot seam rather than implying new baseline churn.

- 2026-05-17: When a backend cleanup slice reuses an already-tracked composition seam for default service glue, continuity should log only the owning guard plus the focused unittest slice that protects the preserved compatibility wrapper, rather than implying wider stability-baseline churn.

- 2026-05-17: When a backend cleanup slice extracts shared router composition but live delivery still belongs to `session_routes.py`, continuity should correct the stability guard to track the real extracted owner instead of implying the composition helper owns stream behavior.
- 2026-05-17: When analyzer diagnostics still report a removed local after a cleanup pass, continuity should prefer direct file read plus grep over reopening the same fix; if the assignment is gone, log the warning as stale analyzer output and keep the cleanup slice closed.
- 2026-05-17: When a cleanup extraction lands cleanly but stability scenarios drift afterward, continuity should record whether the real fix is harness ownership or projection scope rather than implying contract drift; if the contracts stayed stable, refresh only the affected baselines and keep the cleanup batch marked green once the focused rerun passes.
- 2026-05-17: When the external review calls out monolithic files, continuity should record the first safe extraction seam that actually landed, the exact narrow validation that stayed green, and any compatibility wrappers preserved, instead of restating the whole review as if the broad decomposition already happened.
- 2026-05-17: Once a cleanup batch becomes the active focus, keep the continuity split explicit: safe refactor work can be marked green after targeted validation and review, while separate live issues such as an offline backend bridge or a weak neutral clip stay tracked as adjacent follow-up seams rather than regressions in the extraction slice.

- 2026-05-14T08:57:41.6820932+01:00: For an empty GitHub repo, the first non-interactive publish path is `git remote add origin <repo>`, `git branch -M main`, `git add -A`, `git commit -m "chore: initial project scaffold"`, then `git push -u origin main`.
- 2026-05-14T08:57:41.6820932+01:00: In Windows PowerShell, quote the upstream ref when validating tracking with git, for example `git rev-parse --abbrev-ref --symbolic-full-name "@{u}"`, or PowerShell will parse `@{...}` as a hashtable.
- 2026-05-14T08:57:41.6820932+01:00: Team focus moved from setup into architecture and phased delivery planning.
- 2026-05-14T08:57:41.6820932+01:00: UniVRM 1.0 is the user-approved baseline for avatar packaging and interchange.
- 2026-05-14T08:57:41.6820932+01:00: The refined 2026 blueprint baseline explicitly names GPT-SoVITS, Faster-Whisper Medium with Small fallback, LLaMA 3.1 8B Q4_K_M, MediaPipe Face Mesh, optional CLIP, and SQLite plus ChromaDB or FAISS as the planning contract for the Windows 10/11 local stack.
- 2026-05-14T08:57:41.6820932+01:00: Team context should describe the primary runtime as a voice-first loop with vision treated as an optional, non-blocking enrichment path rather than a core dependency.
- 2026-05-14T08:57:41.6820932+01:00: The agreed 2026 stage order is Stage 0 contracts, backend skeleton, frontend VRM rendering, STT plus TTS, local LLM plus memory, animation DSL, vision pipeline, character swapping, then optimization and polish.
- 2026-05-14T08:57:41.6820932+01:00: Stage 1 execution focus is now locked around three concrete seams only: backend contract normalization, one manifest-derived default-character VRM shell in the frontend, and deterministic backend stability baselines.
- 2026-05-14T08:57:41.6820932+01:00: Stage 1 decision merges should leave the broader Trinity batch-handoff inbox note intact unless the request explicitly asks for that contract-level consolidation too.
- 2026-05-15: The landed animation seam is backend-owned and engine-neutral: semantic ids resolve through shared inventory, declared character overrides, then safe fallback, while the promoted DSL schema stays distinct from staged provenance sidecars and that boundary is now stability-checked.
- 2026-05-15: The current viewer default-base path is now explicit and repo-backed: Unity batch export can emit a generated `idle.default` runtime payload plus semantic candidate, and the avatar runtime falls back to `idle.default` after load when no pending or active base animation has already been set.
- 2026-05-15: When a live browser check proves a narrow frontend playback fix, continuity should record the fix boundary and explicitly fence unrelated visible failures such as backend speech lifecycle 422s so the current blocker does not drift back to an already-resolved seam.
- 2026-05-15: When a refresh regression is reproducible but the loaded debug snapshot still contains the expected code edit, continuity should record the root cause as a runtime binding or payload issue rather than implying a lost refresh state.
- 2026-05-15: When exporter-side animation work is validated end to end, continuity should record the source-data proof, the explicit Unity editor path used to refresh runtime payloads, and the exact lower-arm channels visible in live `/display/` debug state so the remaining risk stays framed as fidelity rather than transport.
- 2026-05-15: When live verification succeeds through a newly preferred runtime path that the current debug inventory does not enumerate, continuity should say so explicitly and treat the screenshot or direct pose check as the stronger evidence until the debug surface is updated.
- 2026-05-15: When a later audit widens the verified export surface beyond the currently bound runtime surface, continuity should record both the preserved architecture boundary, such as a semantic-only backend, and the exact exported-but-intentionally-unbound channel families so follow-up work does not accidentally widen behavior.
- 2026-05-15: Once both finger stretch and conservative finger spread are bound, continuity should stop framing hands as missing basic connections and instead describe the remaining hand work as optional higher-fidelity transform hints or broader region coverage.
- 2026-05-15: Once a dev-only browser override exists for backend live versus forced reference clips, continuity should record that seam as a validation tool for future shoulder, elbow, and wrist tuning rather than continuing to describe punch A/B playback as blocked by missing UI control.
- 2026-05-15: When a live `/display/` tuning pass uses a user-supplied target screenshot, continuity should record the concrete target frame, the exact clip-local weights that changed, the display surface used for validation, and whether the remaining mismatch has narrowed from delivery defects to finer pose calibration.
- 2026-05-15: When only one inbox note from an investigation is a durable implementation decision and the rest are diagnostic evidence or falsifiers, merge just the durable fix into `.squad/decisions.md` and preserve the supporting investigation trail in `now.md`, logs, and agent histories instead of promoting every note to an active decision.
- 2026-05-15: When a focused stability rerun after a runtime playback fix shows only one intentional punch-snapshot drift, record the exact field-level seam that moved, refresh just that baseline, and keep the next continuity step on the still-unverified backend-driven path rather than reopening the already-validated local override slice.
- 2026-05-15T23:14:51.4365825+01:00: If both `GET /session/animation` and `PUT /session/lifecycle-state` time out with zero bytes on `127.0.0.1:8000`, treat stale uvicorn listener ownership as the first discriminator; if direct `:8000` probes recover after replacing the owner but the shell path still fails, move the live diagnosis to the frontend dev proxy seam.
- 2026-05-16: If `gesture.punch.once` has a dedicated exporter/runtime repair branch and `idle.default` does not, continuity should treat idle follow-up as an explicit asset-versus-pipeline decision. Record the available inspection control, state that the punch repair path is not shared by default, and leave the next step as a conscious choice between widening the repair path or replacing the idle source clip.
