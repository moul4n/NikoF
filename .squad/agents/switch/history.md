# Project Context

- **Owner:** Jason Fletcher
- **Project:** Local-only anime companion with interchangeable VRM characters, local voice pipeline, persistent memory, and AI-authored animation scripts
- **Stack:** Python backend, FastAPI/Starlette, React + TypeScript + Vite, three.js + three-vrm, Faster-Whisper, Ollama/llama.cpp, GPT-SoVITS, SQLite, Chroma/FAISS
- **Created:** 2026-05-14T08:57:41.6820932+01:00

## Learnings

- Frontend must support VRM swapping, shared animation libraries, and optional character-specific overrides.
- Expression, viseme, and idle animation channels need clean APIs so the backend can drive them predictably.
- Placeholder frontend catalog data should stop at manifest entry points; resolve model, metadata, voice, and override URLs relative to each manifest so Phase 1 does not bake filesystem branching into UI code.
- Avatar viewer work should hang off stable mount point ids and a small runtime bridge now, leaving the React shell and selection flow intact when three.js plus three-vrm arrives.
- Stage 2 default-character work can keep the manifest contract authoritative while still bundling one real VRM by overriding only the manifest-declared `model.vrm` path with a Vite-imported asset URL for `test-vrm-01`.
- `getAvatarRuntimeMountPoints()` must return a stable object identity because the stage mount effect depends on it; otherwise React re-renders remount the canvas and reset runtime state.
- Stage 1 frontend-to-backend bridging should overlay backend character summaries and active-character session state onto the local manifest catalog by `character_id`, never replace manifest-derived asset URLs with backend payloads.
- The frontend shell can persist active-character changes over HTTP, but it must degrade cleanly to local-only selection when `/characters` or `/session/active-character` is unavailable.
- In Vite dev, route backend bridge calls through a local `/api` proxy by default so the shell can use FastAPI surfaces without opening CORS work in the frontend slice.
- The backend-owned `GET /session/speech-lifecycle` surface can be consumed as a pull-only snapshot in the shell once the manifest catalog is ready; refresh it after backend-confirmed active-character outcomes so the displayed lifecycle state stays aligned without adding polling or live transport.
- Surface canonical transcription and synthesis state from the normalized snapshot envelope itself, including cursor order and profile metadata, instead of inventing frontend-specific speech state models before SSE or WebSocket delivery exists.
- 2026-05-15: In `frontend/src/avatar/runtime/humanoidChannelPlayback.ts`, reducing shoulder `down_up` lift and increasing upper-arm `down_up` drop was not sufficient to lower the idle arms; the decisive arm-only correction was flipping only the upper-arm `down_up` signs while leaving torso, head, and shoulder mappings otherwise intact.
- 2026-05-15: Live `/display/` verification is the discriminating check for generated humanoid playback tuning: once the upper-arm `down_up` signs were flipped, the arms settled by the sides while the broader body and head improvements held, shifting the remaining risk toward playback polish and exporter fidelity rather than basic arm calibration.
- 2026-05-15: If a raised-arm regression reappears after refresh, check the loaded debug snapshot before assuming a lost edit. In this batch the sign-flip change was still present, and the actual cause was the leftover `inputOffset: 1` on upper-arm `down_up`, which re-lifted authored negative idle samples.
- 2026-05-15: The current generated idle payload exposes useful forearm twist and largely static stretch, but not a meaningful elbow-flex channel for this clip. Small lower-arm twist is the safe elbow-area polish path until exporter fidelity or channel content improves.
- 2026-05-15: When the source clip still carries lower-arm signal in `left/right.forearm.stretch`, the right next step is exporter-side derivation of explicit `left.elbow.flex` and `right.elbow.flex` channels plus data-driven lower-arm bindings in the web path, not more frontend-only elbow heuristics.
- 2026-05-15: When the raw Unity humanoid clip still does not expose direct lower-arm bone transform curves, exporter-composed `left/right.lower_arm.rotation.{x,y,z,w}` quaternion hints are the smallest honest bridge to better web playback; the runtime should prefer those hints when present while true avatar-backed bone-local export remains a separate fidelity step.
- 2026-05-15: When generated `idle.default`, `listen.loop`, and `speak.loop` payloads already show finite, non-trivial spread samples for every digit, the smallest honest spread step is root-only binding: thumb spread on the metacarpal and index, middle, ring, and little spread on proximal finger bones while preserving all existing torso, arm, wrist, lower-arm, and finger stretch behavior.
- 2026-05-15: When `gesture.punch.once` already resolves locally but the live display surface needs a discriminating wrist and lower-arm check, a dev-only App-level override with `Backend live`, `Force idle.default`, and `Force gesture.punch.once` is the narrowest useful seam. Re-clickable forced punch playback is enough to confirm runtime delivery without widening backend animation ownership.
- 2026-05-15: When Jason supplies a final-frame screenshot for `gesture.punch.once`, the cleanest web-side correction is to keep the target pose in live `/display/`, reduce shoulder `front_back` carry, slightly firm hand `down_up` and `in_out`, and strengthen punch-only arm `front_back` while preserving the lower-leg fix and existing finger behavior.
- 2026-05-15: If the punch still finishes with a low guard after that pass and lower-arm quaternion hints remain active, the remaining mismatch is more likely punch-specific upper-arm carry than lower-arm quaternion interpretation; adjust only punch overrides for `arm.down_up` and `arm.front_back`.
- 2026-05-15: If the live avatar still turns away during `gesture.punch.once` while tracked chest, shoulder, upper-arm, lower-arm, and hand local rotations already match Unity comparison data, treat the defect as root/world transform playback. Sample authored `rootq_*` and `roott_*` channels relative to the first frame and convert Unity root space at the avatar root seam before trying another per-bone Z remap.
- 2026-05-17: The current safe App.tsx extraction seam is the dev-only display tooling cluster. Moving the display override options, display debug panels, and punch-comparison polling into one frontend module keeps backend sync and avatar runtime ownership unchanged while removing a few hundred lines of display-only helpers from the shell.
- 2026-05-17: In `frontend/src/avatar/runtime/officialPunchClipPlayback.ts`, the `createOfficialPunchClipPlayback` compatibility alias was fully unused in the workspace while `avatarRuntime.ts` already imported `createOfficialBoneLocalClipPlayback`; a direct search is enough to justify removing the alias without widening runtime behavior.
- 2026-05-17: The next safe `App.tsx` extraction seam after dev-display tooling is character-shell state. Moving catalog load, backend bridge refresh, persisted selection, cross-tab selection sync, and active-character submit flow into `frontend/src/app/useCharacterShellState.ts` cuts App orchestration weight without moving session animation, speech lifecycle, or avatar runtime ownership out of App. A frontend `tsc --noEmit` pass held after the extraction.
- 2026-05-17: `frontend-shell-split-surface` must scan `frontend/src/app` `.ts` and `.tsx` files together, but backend-sync ownership should stay tied to bridge markers such as `ActiveCharacterSyncError`, `bridgeCharacterCatalogWithBackend`, and `syncActiveCharacterSelection` rather than any generic `resolveSelectedCharacterId` call. With that narrower ownership rule, `useCharacterShellState.ts` becomes the sole backend-sync owner, `App.tsx` remains the sole `speech.lifecycle` owner, and `devDisplayTools.tsx` stays presentation-only. The focused stability scenario passed after the baseline refresh.
- 2026-05-17: `frontend-stage1-character-flow-runtime` must track `frontend/src/app/useCharacterShellState.ts`, compile that hook path inside the harness, and snapshot hook-owned backend-sync markers alongside the existing bridge and reconciliation runtime proof. Without that compile-plus-marker check, the executable character-flow guard can keep passing on helper-level behavior while missing the extracted shell seam entirely.
- 2026-05-17: The next safe App-owned session seam after character-shell state is speech lifecycle transport state. Moving the load, live-consumption, offline-retry, and state-message cluster into `frontend/src/app/useSpeechLifecycleState.ts` keeps App as the owner of canonical synthesis playback handoff, display reconciliation, and conversation animation orchestration while reducing shell weight. The extraction held under `npm exec -- tsc --noEmit` plus refreshed `frontend-shell-split-surface` and `frontend-speech-lifecycle-runtime` baselines.
- 2026-05-17: The next safe App-owned session seam after speech lifecycle is session animation transport state. Moving the live-consumption, backend lifecycle update reconciliation, selection catch-up, and offline-retry cluster into `frontend/src/app/useSessionAnimationState.ts` keeps App as the owner of final runtime play decisions and conversation lifecycle intent while reducing shell weight. A frontend `tsc --noEmit` pass held after the extraction, and `frontend-shell-split-surface` is the narrow stability seam that now needs to track the new hook owner.
- 2026-05-17: The next safe App-owned seam after session animation transport is canonical synthesis playback handoff. Moving the Audio-plus-timing fallback bridge into `frontend/src/app/useSpeechPlaybackBridge.ts` keeps `App.tsx` as the owner of canonical synthesis selection, runtime wiring, and conversation lifecycle intent while shrinking the remaining shell orchestration. A frontend `tsc --noEmit` pass held after the extraction, and the affected stability update is to track the new hook in `frontend-shell-split-surface` plus move the speech-playback handoff markers in `frontend-speech-lifecycle-runtime` from `App.tsx` to the new hook.
- 2026-05-17: The next safe App.tsx seam after the speech playback bridge is presentation-only surface composition. Moving the inline surface panels plus UI-only helpers such as `buildSurfaceHref`, display reply formatting, and playback-status labeling into `frontend/src/app/surfaceShellPresentation.tsx` shrinks App without moving runtime, transport, or branch ownership out of the top-level shell. A frontend `tsc --noEmit` pass held after the extraction, and `frontend-shell-split-surface` is the affected stability seam because its tracked inputs now need to include the new presentation module.
- 2026-05-17: The next safe App.tsx seam after the first presentation extract is top-level shell branch composition. Moving the full control and display render trees plus their UI-only prop assembly into `frontend/src/app/surfaceShellPresentation.tsx` keeps `App.tsx` as the runtime owner and final `surfaceMode` selector while shrinking the remaining shell orchestration. The local harness follow-up is that `frontend-shell-split-surface` must distinguish the selector in `App.tsx` from the render-tree owner in `surfaceShellPresentation.tsx` instead of hardcoding both responsibilities to `App.tsx`.
- 2026-05-17: The next safe App.tsx seam after top-level branch composition is derived shell orchestration. Moving dev display override state, display-side speech lifecycle reconciliation, latest-published speech cursor catch-up, and conversation lifecycle derivation into `frontend/src/app/useSurfaceShellOrchestration.ts` keeps `App.tsx` as the runtime owner of snapshot subscription, character load, and final playback decisions while shrinking the remaining shell glue. A frontend `tsc --noEmit` pass held after the extraction, and `frontend-shell-split-surface` is the narrow stability seam that now needs to track the new orchestration hook.
- 2026-05-17: The next safe App.tsx seam after derived shell orchestration is the runtime-adjacent shell effect cluster that does not choose the final playback command. Moving runtime snapshot subscription, unmount cleanup, display debug and playback-path synchronization, and selected-character load priming into `frontend/src/app/useAvatarRuntimeShell.ts` keeps `App.tsx` as the runtime owner and the sole `runtime.play(...)` decider while shrinking the remaining shell glue. The affected stability update is to track the new hook in `frontend-shell-split-surface` and `frontend-avatar-idle-default-runtime`; both refreshed green, while a repo-wide frontend `tsc --noEmit` pass is currently blocked by the unrelated missing `isIdleSemanticAnimationPayload` symbol in `frontend/src/avatar/runtime/avatarRuntime.ts`.
- 2026-05-17: `frontend-avatar-idle-default-runtime` should stay seam-scoped to the `App.tsx` and `useAvatarRuntimeShell.ts` ownership split: the hook owns runtime snapshot subscription, unmount cleanup, display configuration sync, and selected-character idle priming, while `App.tsx` remains the sole `runtime.play(...)` decider. Do not refresh that baseline with backend session documents, generated runtime sidecars, or `avatarRuntime` payload proofs; those belong to other stability seams and create false churn during App-shell extraction cleanup.
- 2026-05-17: The next safe App.tsx seam after `useAvatarRuntimeShell.ts` is runtime playback input preparation. Moving dev-display option resolution, display-readiness gating, backend snapshot handoff, and offline idle eligibility into `frontend/src/app/useRuntimePlaybackSelection.ts` trims App without moving the explicit `runtime.play(null | command | backend snapshot | local idle)` branches or top-level surface selection out of `App.tsx`. The narrow stability follow-up is to track the new hook in `frontend-shell-split-surface` while keeping `frontend-avatar-idle-default-runtime` scoped to App plus `useAvatarRuntimeShell.ts`.
- 2026-05-18: For the control-surface operator preview, the right read model is the existing `speech.lifecycle` snapshot plus live-delivery state, not a panel-local reply store. Extend `consumeSpeechLifecycleSnapshot()` to surface the latest `assistant.message` and latest event, keep `/session/operator-command` as the only write path, and let the panel fall back to the accepted POST response only until the canonical lifecycle cursor catches up. A frontend `npm exec -- tsc --noEmit` pass held after wiring the panel to the canonical lifecycle snapshot.
- 2026-05-18T10:14:35Z: When the Stage 1 Node harness needs rendered prerequisite-card proof but the full control shell is blocked by extensionless browser-side TSX imports, keep the proof on `frontend-stage1-character-flow-runtime` by extracting the production `ControlSurfaceSummaryPanel` into a tiny renderable module, rendering that real card markup under Node, and separately asserting `ControlSurfaceShell` still mounts the same panel. This preserves rendered-text coverage without adding Playwright or a second browser stack.
- 2026-05-18: For frontend TTS preview, never convert a machine-local `audio_reference` into a `file:///` browser shortcut. Treat `speech.lifecycle` as the authoritative read model, play only browser-safe URLs, clear speech reactions on both audio completion and timing completion, and fall back to canonical timing metadata with explicit operator feedback when the backend only publishes a local filesystem path. The focused `frontend-speech-lifecycle-runtime` stability scenario passed after this fix.
- 2026-05-18: If `idle.default` drift is fixed but feet still float on the official idle route, check whether the runtime is filtering out exported lower-body comparison bones. Keeping upper-leg, lower-leg, foot, and toe local rotations while root motion stays disabled preserves planted contact, and any fake-VRM stability harness must expose that same leg chain or it will under-report the fix.
- 2026-05-18: If `idle.default` already exports `left/right.elbow.flex` and lower-arm comparison quaternions but the elbow still reads weak, treat it as an official-idle playback-weighting problem before reopening exporter or App routing. A modest lower-arm pitch-first scale in `officialPunchClipPlayback.ts`, validated by the existing `frontend-avatar-idle-default-runtime` lower-arm follow-through slice, is the smallest honest elbow pass when settle and grounding proof stay unchanged.
- 2026-05-18T10:58:02.4627970Z: If `idle.default` already carries explicit `LeftHand` and `RightHand` local rotations on `official_idle_stability` but the wrists still read flat after an elbow pass, treat it as hand-weighting rather than route or exporter drift. A modest hand pitch-first scale in `officialPunchClipPlayback.ts`, validated by the existing `frontend-avatar-idle-default-runtime` hand follow-through slice and unchanged grounding proof, is the smallest honest wrist pass before widening into finger or browser-proof work.
- If `idle.default` already ships non-trivial finger channels but the official idle rendered seam still stops at hands, treat finger flatness as dropped official-idle root-finger routing first. The smallest honest pass is to admit only thumb metacarpal plus index, middle, ring, and little proximal bones on `official_idle_stability`, then extend `frontend-avatar-idle-default-runtime` to require that same root-finger slice to be targeted, show dominant-axis sign agreement, and settle back near loop start.
- 2026-05-18T11:27:39.8179670Z: If the approved official-idle root-finger slice still lacks comparison quaternions in the checked-in sidecar, keep the repair local to `frontend/src/avatar/runtime/officialPunchClipPlayback.ts` instead of reopening exporter or backend transport work. Bind only the approved root fingers from the existing `*.stretched` and `*_spread` runtime muscle channels on `official_idle_stability`, and leave approval on `frontend-avatar-idle-default-runtime` only once the strengthened hand-space articulation proof and loop-start settle checks stay green.
- 2026-05-18T11:22:18.2082733Z: For the next idle finger hardening pass, no new frontend hook is needed if a browser-adjacent check is acceptable. The existing `frontend-avatar-idle-default-runtime` seam already samples loop start, one proof timestamp, and final frame on `official_idle_stability`, records root-finger rendered articulation in hand space, and passed as the cheapest discriminating check. Only add a live browser debug hook if the ask explicitly widens to real-VRM morphology proof in the display surface, because the current `window.__NIKOF_AVATAR_DEBUG__` API is still punch-specific for pose comparison.
