# Project Context

- **Owner:** Jason Fletcher
- **Project:** Local-only anime companion with interchangeable VRM characters, local voice pipeline, persistent memory, and AI-authored animation scripts
- **Stack:** Python backend, FastAPI/Starlette, React + TypeScript + Vite, three.js + three-vrm, Faster-Whisper, Ollama/llama.cpp, GPT-SoVITS, SQLite, Chroma/FAISS
- **Created:** 2026-05-14T08:57:41.6820932+01:00

## Learnings

- High-risk paths include end-to-end voice latency, WebSocket payload drift, and character swaps that break rig or animation assumptions.
- The project needs executable checks around shared versus character-specific animation behavior early, not only at polish time.
- 2026-05-18T12:12:53Z: Once Jason explicitly chose real-VRM morphology hardening after the approved root-finger slice, the smallest honest frontend pass was a dev-only `getIdleFingerSnapshot()` sampler on `window.__NIKOF_AVATAR_DEBUG__` sourced from `frontend/src/avatar/runtime/avatarRuntime.ts` and surfaced through the current display rail in `frontend/src/app/devDisplayTools.tsx`, `frontend/src/app/App.tsx`, and `frontend/src/app/surfaceShellPresentation.tsx`. Sample loop start, quarter, mid, three-quarter, and loop return on the official idle path, keep `frontend-avatar-idle-default-runtime` green as the authoritative seam, and treat any `frontend-shell-split-surface` snapshot drift as collateral unless backend-sync or lifecycle ownership actually moved.
- 2026-05-18T11:07:09.6816356Z: Approve a local `idle.default` wrist weighting retune on the existing `frontend-avatar-idle-default-runtime` seam when `frontend/src/avatar/runtime/officialPunchClipPlayback.ts` only adds modest `LeftHand` and `RightHand` weighting on `official_idle_stability`, the runtime scenario still passes with hand-axis sign proof, sampled rendered excursion, loop-start settle, and grounding intact, and the frontend TypeScript no-emit compile stays clean; the remaining risk is still fake-rig sampled-pose coverage for morphology-specific wrist silhouette drift or yaw-roll defects.
- 2026-05-18T10:58:02.4627970Z: For an `idle.default` wrist slice, `frontend-avatar-idle-default-runtime` is still the cheapest honest approval seam when the change stays inside `frontend/src/avatar/runtime/officialPunchClipPlayback.ts` and only retunes `LeftHand` or `RightHand` on `official_idle_stability`; require extra proof only if the slice widens into finger bones, route or target-set drift, or wrist yaw or roll behavior that the current hand proof does not explicitly constrain.
- 2026-05-18T10:48:09.9571509Z: Approve an `idle.default` elbow weighting retune on the existing `frontend-avatar-idle-default-runtime` seam when the official idle route, targeted-bone boundary, lower-arm follow-through booleans, and grounded-contact booleans stay green; treat fake-rig sampled-pose coverage as the remaining risk rather than reopening exporter or App seams.
- 2026-05-18T10:39:11.4419754Z: For an `idle.default` elbow or lower-arm follow-through slice, start with `frontend-avatar-idle-default-runtime` before inventing any new proof. That seam already constrains `official_idle_stability` lower arms through rendered-pose excursion, authored-axis sign agreement, and loop-start settle checks; require extra proof only if the change escapes `frontend/src/avatar/runtime/officialPunchClipPlayback.ts` or widens beyond the existing lower-arm follow-through boundary.
- 2026-05-18T07:25:39Z: When bootstrap prerequisite coverage expands from coarse folder checks to hook-backed `hint`, `runtime.json`, and `install-plan.json` manifests, repair the focused startup test to the explicit local-path contract instead of weakening it to whatever the current startup banner happens to print.
- Real VRM intake should stay anchored to `model.vrm` at each package root; changing file names or adding extra nesting would break the manifest contract before runtime code exists.
- Animation policy is easier to enforce when each storage root carries its own README describing promotion versus override rules, instead of relying on one top-level note.
- When a vendor VRM arrives with an arbitrary export filename, normalize the package root back to `model.vrm` immediately; under the current scaffold schema, keep the runtime filename in `source_vrm.file_name` and stash the dropped vendor filename as explicit intake provenance in `source_vrm.embedded_identifier` until reviewed identity metadata replaces the scaffold.
- 2026-05-14: The low-friction regression seam is a PowerShell harness that snapshots existing repo scripts into checked-in JSON baselines and leaves run artifacts untracked under `tests/stability/artifacts/`.
- 2026-05-14: Baseline refresh must stay explicit; the harness should default to diff mode and only rewrite checked-in expectations behind `-RefreshBaselines`.
- 2026-05-14: The Stage 1 backend stability seam should call the backend-owned `build_api_contract_snapshot()` helper instead of reconstructing route behavior in the harness; that keeps route registration and normalized response payloads aligned with the app module even when FastAPI is absent.
- 2026-05-14: Stabilize backend snapshots by sandboxing `NIKOF_*` local-root environment variables and normalizing dynamic session-event timestamps to `<generated-at>`; otherwise `GET /health` and active-character event payloads drift per machine and per run.
- 2026-05-14: When a JSON value contract is not locked yet, snapshot the stable key surface separately instead of inventing provisional error payloads; once the backend owns a rejection contract on the branch, fold that exact response into the widened-payload baseline instead of stubbing it in the harness.
- 2026-05-14T08:57:41.6820932+01:00: Stability compare mode should canonicalize JSON before diffing; otherwise PowerShell serializer whitespace changes create false failures even when the underlying object graph is unchanged.
- 2026-05-14T08:57:41.6820932+01:00: `bootstrap-prerequisites` should snapshot declared tool requirements from `bootstrap.targets.json`, not live command availability, because PATH state is machine-local noise while tool ids and commands are the contract worth guarding.
- 2026-05-14T08:57:41.6820932+01:00: Speech event-store stability coverage can stay deterministic before a durable store exists by projecting the backend-owned `speech_lifecycle_transport_snapshot` into persisted-record and cursor-read views instead of inventing a second envelope.
- 2026-05-14T08:57:41.6820932+01:00: Backend stability snapshots should normalize the Python launcher label to `python`; otherwise machines that fall back to `py -3` create false diffs unrelated to contract drift.
- 2026-05-14T08:57:41.6820932+01:00: The degraded real-adapter speech baseline should snapshot current backend behavior, including provider entrypoint selection and `unavailable` contract statuses when local STT or TTS payloads are absent, rather than forcing stub-ready values.
- 2026-05-14T08:57:41.6820932+01:00: Frontend source-inspection scenarios have to follow the real owner of the bridge behavior. If envelope reads or rejection rollback move into helpers such as `backendCharacterFlow.ts`, update both the matcher and tracked inputs there instead of treating the extraction as contract drift.
- 2026-05-14T08:57:41.6820932+01:00: When a runtime stability scenario still proves the same behavior but its tracked-input contract expands, refresh only that scenario baseline intentionally with `-RefreshBaselines` rather than widening compare logic or rewriting unrelated baselines.
- 2026-05-14T08:57:41.6820932+01:00: Backend unit tests imported as `from app...` should bootstrap `backend/` onto `sys.path` inside the narrow test slice when they need to pass under repo-root `python -m unittest discover backend/tests`; otherwise discovery depends on a backend-local working directory.
- 2026-05-14T09:11:00+01:00: Guard the turn-publication seam with one explicit empty-snapshot-before-publication assertion plus a dedicated stability scenario that compares snapshot event ids against publisher output; otherwise helper snapshots like `build_api_contract_snapshot()` can hide accidental snapshot-read seeding behind prepopulated test state.
- 2026-05-14T09:18:00+01:00: When `backend-stage1-contracts`, `backend-speech-contracts`, and `backend-speech-event-store` all drift together after a speech publication change, first prove the diffs are confined to publication-owned `status` and `reason` fields from `DefaultTurnPipelinePublisher`; if route shape and payload keys are unchanged, refresh only those baselines and rerun the narrow trio before the full suite.
- 2026-05-14T09:31:00+01:00: If backend live delivery is still unlanded, prep the next transport batch by freezing the existing `speech.lifecycle` cursor and top-level envelope fields in narrow backend tests and validate against `backend-speech-event-store`; do not invent a transport stability scenario until the backend owns an actual stream surface.
- 2026-05-14T09:36:00+01:00: For optional FastAPI surfaces, keep route-level transport coverage dependency-light by patching a tiny fake `fastapi` module and calling the real route handler directly; that still proves content negotiation, SSE framing, cursor resume, and invalid-cursor rejection without forcing framework installation into the backend test slice.
- 2026-05-14T10:03:00+01:00: When the frontend control/display split has not landed yet, prep it with one source-inspection stability scenario that records which `frontend/src/app/` file owns backend sync and `speech.lifecycle` consumption. Baseline the blocked state now, then refresh that same scenario once Switch adds presentation surfaces so duplicate control paths fail without inventing a router or second transport contract.
- 2026-05-14T10:08:00+01:00: Once the shell exposes control and display surfaces behind App-owned branching, keep the frontend guard focused on single ownership of backend sync and `speech.lifecycle` consumption rather than requiring router seams or a second sync path. Follow-up display-only extraction should refresh the same ownership baseline instead of widening transport coverage.
- 2026-05-14T10:17:00+01:00: For the real /control and /display entrypoint batch, `frontend-shell-split-surface` should snapshot top-level `frontend/src/*.tsx` entrypoints separately from `frontend/src/app/App.tsx`. Baseline the blocked one-entrypoint state now, then refresh the same scenario once Switch lands the split so every entrypoint must still render `App` while `App.tsx` remains the sole owner of backend sync and `speech.lifecycle`.
- 2026-05-14T10:23:00+01:00: With the real entrypoints landed, keep `frontend-shell-split-surface` focused on the same ownership rule. Only widen its tracked inputs if later display-only composition moves presentation code under `frontend/src/app/`; do not turn it into a router, transport, or second-sync-path guard.
- 2026-05-14T10:46:00+01:00: The operator command client must not live in `App.tsx`; keep its locale, drafts, and submit mutation state in a control-only component and let `frontend-shell-split-surface` lock the single owner, backend seam path, and the allowed `text_question` plus `tts_preview` command types.
- 2026-05-15: When live `/display/` still turns the avatar away but the browser-versus-Unity punch comparison shows final-frame local chest, shoulder, arm, and hand rotations matching to float-noise tolerance, the next honest discriminator is root/world transform playback. Check for authored `RootQ.*` and `RootT.*` channels reaching the frontend before reopening any general Z-axis inversion theory.
- 2026-05-18T10:15:34.1052229Z: `frontend-avatar-idle-default-runtime` can pass while idle feet visibly float because its current rendered-pose proof stays on torso and arm rotations. Keep the same stability scenario, but add a grounded-foot runtime assertion that samples `LeftFoot`, `RightFoot`, `LeftToes`, and `RightToes` world-space Y after idle grounding and fails if any grounded contact point rises above a tight loop-start epsilon.
- 2026-05-18T10:26:38.8609715Z: For idle grounding approval, require the runtime and the harness to share the same feet-preferred floor-height rule and accept on two conditions: the sampled grounded minimum still touches floor and no foot-or-toe contact point drifts upward from the grounded loop-start sample. Exact per-bone loop-start floor touch is only a harness-sanity signal and should not block approval by itself.
- 2026-05-18T11:22:18.2082733Z: `frontend-avatar-idle-default-runtime` remains the cheapest idle review seam for fingers, but its current rendered proof boundary stops at `leftHand` and `rightHand`; the baseline also snapshots exported finger channels without proving rendered finger articulation, so finger approval needs a focused extension on the same scenario rather than green hand checks alone.
- 2026-05-18T11:27:39.8179670Z: Approve the revised `idle.default` root-finger slice on the existing `frontend-avatar-idle-default-runtime` seam when `frontend/src/avatar/runtime/officialPunchClipPlayback.ts` admits only the approved root fingers and binds them from runtime channels if comparison quaternions are absent, the scenario still passes with explicit per-finger hand-space articulation vectors, rendered articulation excursion above threshold, dominant-axis sign agreement, loop-start settle, and unchanged grounding, and the frontend TypeScript no-emit compile stays clean; the remaining risk is morphology-specific or transient finger silhouette weakness on real VRMs, especially for digits whose sampled articulation stays close to the epsilon on the fake-rig seam.
- 2026-05-18T11:22:18.2082733Z: When the fake-rig idle finger seam is already green but the remaining risk is real-VRM morphology, the narrow next hardening step can stay on current frontend surfaces by adding a dev-only `window.__NIKOF_AVATAR_DEBUG__.getIdleFingerSnapshot()` sampler in `frontend/src/avatar/runtime/avatarRuntime.ts` and surfacing it in the display rail. That gives real-VRM root-finger checkpoints at loop start, quarter, mid, three-quarter, and loop return without adding Playwright or a second browser runner; keep the existing `frontend-avatar-idle-default-runtime` scenario green in the same pass.
