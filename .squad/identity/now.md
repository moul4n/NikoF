Current Focus
=============

updated_at: 2026-05-14T15:45:00+01:00
focus_area: The display surface regression is fixed on the current read-only frontend path, and the next session should apply the base idle animation to the model without widening scope.
active_issues: []
---

What We're Focused On
---------------------

The current handoff point is narrow. The display surface regression is fixed on the existing read-only frontend path, and the next session should start by applying the base idle animation to the model through the current asset pipeline without reopening the recently settled display shell work.

Keep the follow-on queue narrow and non-debug. Preserve the existing operator-command path and command types, keep active-character selection as the only selection control, and continue treating full phoneme inference, richer facial animation beyond the base idle application step, transport expansion, broader memory orchestration, embeddings or vector retrieval, cross-session affinity, UI-visible memory diagnostics, provider-profile switching, animation debug actions such as `wave`, extra control-surface debug toggles or diagnostics, and any new operator command beyond the landed `text_question` and `tts_preview` pair as out of scope. Updated by Scribe after the display-surface repair was captured for handoff and the next-session idle-animation step was queued.
