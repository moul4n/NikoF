updated_at: 2026-05-14T10:08:00+01:00
focus_area: Frontend live speech lifecycle consumption and the control-versus-display shell split are both landed on the existing App-owned backend sync and `speech.lifecycle` loader path, the new shell-split guard is in place, and the next seam is display-only composition on the current local `surface` branch without adding router-level navigation or widening backend contracts
active_issues: []
---

# What We're Focused On

The current repo state now includes a backend-owned event store for canonical session and `speech.lifecycle` events, configuration-aware Faster-Whisper and GPT-SoVITS adapter execution paths behind provider-agnostic speech interfaces, an explicit backend turn-publication path that appends ordered session and speech lifecycle events, backend live delivery on the existing `GET /session/speech-lifecycle` surface via SSE content negotiation, and a frontend shell that boots from the backend snapshot contract, upgrades into the same SSE surface when live delivery is available, and keeps that backend-confirmed state centralized in `App.tsx` even after the shell now branches into explicit control and display surfaces. The full validation set remains green across the frontend build and PowerShell stability suite, including the runtime proof that appended `speech.lifecycle` envelopes advance cursors and update the canonical synthesis event without inventing a second transport contract, plus the new source-inspection guard that keeps backend sync and speech lifecycle ownership on one path while the presentation surface stays separate. The next seam is narrower than router work: deepen display-only composition on the existing local `surface` branch first, and only introduce deep-linkable navigation if the information architecture becomes real enough to justify a routing dependency.
Updated by Scribe after the shell-split batch landed to keep the current frontend focus aligned with the published control/display split, the new stability guard, and the still-unchanged backend envelope boundary.
