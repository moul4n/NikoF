updated_at: 2026-05-14T09:53:00+01:00
focus_area: Frontend live speech lifecycle consumption is now landed on the existing `GET /session/speech-lifecycle` SSE surface with snapshot bootstrap and fallback preserved, the canonical `speech.lifecycle` envelope still spans both snapshot and live delivery, runtime and stability coverage now prove appended-cursor live updates, and the next seam is end-to-end shell integration on top of this bridge without widening transport or payload contracts
active_issues: []
---

# What We're Focused On

The current repo state now includes a backend-owned event store for canonical session and `speech.lifecycle` events, configuration-aware Faster-Whisper and GPT-SoVITS adapter execution paths behind provider-agnostic speech interfaces, an explicit backend turn-publication path that appends ordered session and speech lifecycle events, backend live delivery on the existing `GET /session/speech-lifecycle` surface via SSE content negotiation, and a frontend shell that now boots from the backend snapshot contract, upgrades into the same SSE surface when live delivery is available, and falls back to the last confirmed snapshot when the live stream is unavailable. The full validation set remains green across the frontend build and PowerShell stability suite, including the runtime proof that appended `speech.lifecycle` envelopes advance cursors and update the canonical synthesis event without inventing a second transport contract. The next seam stays intentionally narrow: wire this live-consumption bridge into the end-to-end shell flow and any follow-on runtime coverage without widening payload shapes or branching away from the backend-owned envelope.
Updated by Scribe to reflect the landed frontend live-consumption batch, the preserved snapshot-plus-SSE envelope contract, and the narrower follow-on focus after this publish.
