{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the NikoF display window. The window is a pure client (no filesystem/shell access); it only loads the bundled frontend, which talks to the backend over HTTP/SSE.",
  "windows": ["display"],
  "permissions": ["core:default"]
}
