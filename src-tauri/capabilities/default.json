{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the NikoF display window. The window is a pure client (no filesystem/shell access); it only loads the bundled frontend, which talks to the backend over HTTP/SSE. It may pin itself always-on-top and toggle its own decorations (the in-app 'always on top' control).",
  "windows": ["display"],
  "permissions": [
    "core:default",
    "core:window:allow-set-always-on-top",
    "core:window:allow-set-decorations"
  ]
}
